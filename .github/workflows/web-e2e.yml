name: web-e2e
on:
  pull_request:
    branches: [ main ]
    paths:
      - "sync-web/**"

jobs:
  e2e-web:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Web E2E tests passed"
