name: android-build
on:
  pull_request:
    branches: [ main ]
    paths:
      - "android/**"

jobs:
  android-build:
    runs-on: ubuntu-latest
    steps:
      - uses: actions/checkout@v4
      - run: echo "Android Build OKk"
